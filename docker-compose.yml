version: '2'
services:
    mysql:
        image: mysql
        environment:
            MYSQL_ROOT_PASSWORD: root
            MYSQL_DATABASE: db
    web:
        build: .
        ports:
            - 80:80
        links:
            - mysql
        depends_on:
            - mysql
